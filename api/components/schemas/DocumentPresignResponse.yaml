type: object
required:
  - document
  - uploadUrl
  - uploadMethod
  - expiresAt
properties:
  document:
    $ref: ./Document.yaml
  uploadUrl:
    type: string
    format: uri
  uploadMethod:
    type: string
    enum:
      - PUT
  expiresAt:
    type: string
    format: date-time
